// Lines that start with a  // are comments in C

// Lines that start with # are preprocessor commands 
#include <stdio.h>

// Start of the main application
int main(int argc, char **argv)
{
	// Prints the text hello world with a new line after it
	printf("Hello world!\n");
	// Where we exit the application by returning a 0
	return 0;
}