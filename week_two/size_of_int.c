#include <stdlib.h>
#include <stdio.h>
#include <limits.h>

int main(int argc, char **argv)
{
	printf("The size of int is %d bytes\n", sizeof(int));
	printf("This is the max int: %d\n", INT_MAX);

	return 0;
}